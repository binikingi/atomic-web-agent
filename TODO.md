# TODO
- [ ] Add centralized logging
- [x] make a way to make the snapshot tool to only capture relevant parts of the DOM
- [ ] Add error handling and recovery mechanisms
- [ ] Add a `verify` function or `check` function on AWAgent for verifing expected behaviors on webpage
- [ ] Add `clear` function on AWAgent to clear chat context
- [ ] Add `extract` function on AWAgent to extract data from page (using zod schema)